# Packages in the ecs-init module that are allowed to have no unit tests.
# These are interfaces, type definitions, generated code, main entrypoints,
# or thin OS wrappers with no testable logic.
#
# If coverage CI fails because a new package has no tests, either:
# 1. Add unit tests for the package (preferred), or
# 2. Add it here with justification if it has no testable logic.
/ecs-init$
/cmd$
/exec$
/version$
/volumes/amazon-ecs-volume-plugin$
/volumes/driver$
