{
  "family": "ecsftest-domainname",
  "containerDefinitions": [{
    "image": "busybox:latest",
    "name": "exit",
    "cpu": 10,
    "memory": 10,
    "domainname": "foobar",
    "command": ["sh", "-c", "[ \"$(hostname -d)\" == \"foobar\" ] && exit 42 || exit 1"]
  }]
}
